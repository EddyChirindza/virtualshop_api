# ── Servidor ──────────────────────────────
PORT=3000
NODE_ENV=development

# ── Base de dados PostgreSQL ──────────────
DB_HOST=localhost
DB_PORT=5432
DB_NAME=virtualshop
DB_USER=virtualshop_user
DB_PASSWORD=troca_esta_password

# ── JWT ────────────────────────────────────
# Gera valores fortes, ex: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=troca_este_segredo_por_algo_forte_e_aleatorio
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_DAYS=30

# ── CORS ───────────────────────────────────
# Domínios/origens permitidas, separadas por vírgula
CORS_ORIGINS=http://localhost:3000

# ── Bcrypt ─────────────────────────────────
BCRYPT_SALT_ROUNDS=12
